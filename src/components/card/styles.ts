import { cva } from "class-variance-authority";

const cardStyles = cva("w-[280px] h-[280px] bg-cyan pl-4");
const cardHeaderStyles = cva("");
const cardContentStyles = cva("");
const cardButtonStyles = cva("");

export { cardStyles, cardHeaderStyles };
